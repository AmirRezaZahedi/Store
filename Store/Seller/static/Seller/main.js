/*const cardContainer = document.getElementById("cardContainer");

const products = [
  // Replace this with your actual product data
  // Each product should have properties like image, name, price, quantity, etc.
  // Example: { image: "image_url", name: "Product Name", price: "$99", quantity: 10 }
  // ...
];

function showProducts() {
  cardContainer.innerHTML = "";
  products.forEach(product => {
    const card = document.createElement("div");
    card.className = "card";
    card.innerHTML = `
      <img src="${product.image}" class="card-image" alt="${product.name}" />
      <h2 class="card-title">${product.name}</h2>
      <p class="card-price">Price: ${product.price}</p>
      <p class="card-quantity">Quantity: ${product.quantity}</p>
      <a href="#"><input type="button" class="btn" value="update"></a>
      <a href="#"><input type="button" class="btn" value="delete"></a>
    `;
    cardContainer.appendChild(card);
  });
  console.log(products);
}

showProducts();*/
